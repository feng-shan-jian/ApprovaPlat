<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.flowable.mapper.WfDeployControlledLoopMapper">
    <resultMap id="LoopResult" type="com.ruoyi.flowable.domain.WfDeployControlledLoop">
        <id property="loopId" column="loop_id"/>
        <result property="deployId" column="deploy_id"/>
        <result property="processKey" column="process_key"/>
        <result property="activityId" column="activity_id"/>
        <result property="activityName" column="activity_name"/>
        <result property="decisionVariable" column="decision_variable"/>
        <result property="repeatValue" column="repeat_value"/>
        <result property="exitValue" column="exit_value"/>
        <result property="maxIterations" column="max_iterations"/>
        <result property="routeVariable" column="route_variable"/>
        <result property="iterationVariable" column="iteration_variable"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="Columns">
        loop_id, deploy_id, process_key, activity_id, activity_name, decision_variable,
        repeat_value, exit_value, max_iterations, route_variable, iteration_variable,
        create_by, create_time
    </sql>

    <insert id="insertBatch">
        <choose><when test="snapshots != null and snapshots.size() > 0">
            insert into wf_deploy_controlled_loop
            (deploy_id, process_key, activity_id, activity_name, decision_variable,
             repeat_value, exit_value, max_iterations, route_variable, iteration_variable,
             create_by, create_time)
            values <foreach collection="snapshots" item="item" separator=",">
                (#{item.deployId}, #{item.processKey}, #{item.activityId}, #{item.activityName},
                 #{item.decisionVariable}, #{item.repeatValue}, #{item.exitValue},
                 #{item.maxIterations}, #{item.routeVariable}, #{item.iterationVariable},
                 #{item.createBy}, current_timestamp(3))
            </foreach>
        </when><otherwise>
            insert into wf_deploy_controlled_loop
            (deploy_id, process_key, activity_id, activity_name, decision_variable,
             repeat_value, exit_value, max_iterations, route_variable, iteration_variable,
             create_by, create_time)
            select '', '', '', '', '', '', '', 2, '', '', '', current_timestamp(3) where 1 = 0
        </otherwise></choose>
    </insert>

    <select id="selectByDeploymentAndProcess" resultMap="LoopResult">
        select <include refid="Columns"/>
        from wf_deploy_controlled_loop
        where deploy_id = #{deployId} and process_key = #{processKey}
        order by activity_id
    </select>

    <select id="selectByDeploymentAndActivity" resultMap="LoopResult">
        select <include refid="Columns"/>
        from wf_deploy_controlled_loop
        where deploy_id = #{deployId} and process_key = #{processKey}
          and activity_id = #{activityId}
    </select>

    <select id="selectByDeploymentId" resultMap="LoopResult">
        select <include refid="Columns"/>
        from wf_deploy_controlled_loop
        where deploy_id = #{deployId}
        order by process_key, activity_id
    </select>

    <delete id="deleteByDeploymentId">
        delete from wf_deploy_controlled_loop where deploy_id = #{deployId}
    </delete>
</mapper>
