# 2026-08-17 工作流架构验收整改验收结果

状态：`blocked`

## 执行命令

以下命令均在当前工作树执行。MySQL 管理口令、管理员测试口令、一次性 Token 和外部端点 API Key 只通过进程环境注入，未写入命令和证据。

```text
mvn test
mvn -Pflowable-it verify
mvn -pl ruoyi-flowable,ruoyi-admin -am '-Dtest=WorkflowDomainArchitectureTest,WorkflowWebArchitectureTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
cd ruoyi-ui; npm run test:contracts
cd ruoyi-ui; npm run lint
cd ruoyi-ui; npm run build:prod
deployment/scripts/run-ui-tests.ps1（受影响通知、协作、SLA、设计器和流程操作集合，执行编号 r12；UI-COLLAB-004 定向复验编号 r13）
mysql ry-vue < sql/flowable/verify/8.0.0__verify.sql
mysql ry-vue < sql/flowable/verify/8.0.0__verify_workflow_business.sql
mysql ry-vue < sql/flowable/verify/8.0.0__verify_workflow_menu.sql
mysqlcheck --check ry-vue
```

## 通过、失败和跳过

| 门禁 | 通过 | 失败 | 错误 | 跳过/警告 |
| --- | ---: | ---: | ---: | ---: |
| 后端全量单元测试 | 1073 | 0 | 0 | 0 |
| `flowable-it` 集成测试 | 127 | 0 | 0 | 0 |
| ArchUnit 领域规则 | 5 | 0 | 0 | 0 |
| ArchUnit Web 规则与构造违规自检 | 2 | 0 | 0 | 0 |
| 前端合同测试 | 91 | 0 | 0 | 0 |
| ESLint | 0 error | 0 | 0 | 106 warning |
| 前端生产构建 | 1 | 0 | 0 | 0 |
| 受影响 Playwright | 26 | 0 | 0 | 1 skipped |

唯一跳过项为 `UI-NOTIFY-005`。当前没有真实短信供应商配置，未执行真实外部短信投递，不以通道桩、源码检查或接口返回代替。

## 数据库

- 最终初始化清单为系统基线、Quartz、Flowable 8 create SQL、模型版本保护、`8.0.0__workflow_business.sql` 和 `8.0.0__workflow_menu.sql` 共九个文件。
- 开发库 `ry-vue` 已直接删除并无备份重建；最终总表数 `94`，其中 `wf_*` 表 `27`，测试账号 `0`。
- 三组 verify 分别为 `5 PASS`、`21 PASS`、`9 PASS`，合计 `35 PASS / 0 FAIL`。
- `mysqlcheck` 为 `94 OK / 0 bad`。
- 隔离 schema `ry_vue_codex_flowable_it`、`ry_vue_codex_rbac_flowable_it` 和 `r5_it_*` 临时账号已删除；Redis DB `0/14/15` 已清空。

## 真实链路对账摘要

- 通知投递：MySQL IT `16/0/0/0`，覆盖领取提交后事务外调用、成功、重试、死信、租约接管、旧 worker 冲突和站内信幂等；外部通道响应只保留稳定分类，不记录正文和凭据。
- 生命周期：MySQL IT `2/0/0/0`，覆盖有界批次、保护状态、协作审计随父删除、SLA audit 级联和双节点并发；清理后未保留 fixture。
- 协作：`UI-COLLAB-001..004` 为 `4 passed`，覆盖顺序消费、重复请求幂等、死信补偿和源实例取消；最终取消状态稳定为 `CANCELLED`，观察窗口内尝试次数和请求数不再增长。
- SLA：`UI-SLA-003` 为 `1 passed`；专项 MySQL IT `4/0/0/0`，页面、API 和数据库状态一致。
- 受影响 Playwright 汇总为 `26 passed / 0 failed / 1 skipped`；浏览器输出、日志、截图和 trace 已删除。

## 删除内容

- 已删除昨日任务目录、数据库 dump、旧 SQL 副本、fixture、日志式证据和过期验收资产。
- 已删除 `8.1.0__workflow_architecture_convergence.sql`，最终结构已合并进 `8.0.0__workflow_business.sql`。
- 已删除旧通知统一门面及旧测试、旧文档和回调包装；生产代码和测试中不再保留旧门面类名。
- 已删除源码字符串合同测试、release gate 自测/evidence 子系统、`FlowableEngineIT` 重复业务场景和 fault-recovery 非浏览器底层故障场景。
- 已删除临时 schema、临时账号、Redis 测试数据、UI 输出、日志、`dist` 和一次性清理脚本。

## 未执行或阻塞

- `blocked`：真实短信供应商投递 `UI-NOTIFY-005`。恢复条件为提供通过环境变量注入的供应商端点、凭据和授权测试号码，然后只重跑该用例。
- `not executed`：真实外部 SMTP 服务端到端验收；现有证据仅覆盖事务外调用和通道桩结果，不声称外部 SMTP 可达。
- `not executed`：workflow release gate 的生产 `preflight`；当前没有不可变发布包、制品摘要和批准锚点。
- `not executed`：生产拓扑和 HA/故障切换验收。

R5 保持 `blocked`，R6 保持 `pending`，不得声明本任务 `completed`。

## 第一阶段纯删除复验

状态：`blocked`

| 门禁 | 本轮结果 | 说明 |
| --- | --- | --- |
| `git diff --check` | passed | 未发现空白错误；工作树既有 LF/CRLF 提示不影响检查结果 |
| Maven 编译与测试源码编译 | passed | `mvn -pl ruoyi-admin -am -DskipTests compile` 与 `test-compile` 均通过 |
| Maven 单测 | `1066/0/0/0` | 8 个 reactor 模块全部成功 |
| `mvn -Pflowable-it verify` | blocked | `102 errors`，均在 Spring 启动阶段由缺少 `FLOWABLE_IT_*`、`FLOWABLE_RBAC_*`、`FLOWABLE_COLLABORATION_*` 环境变量触发 |
| 前端合同测试 | `39/0/0` | 删除失效入口后不存在指向缺失文件的 package script |
| ESLint | `0 error / 104 warning` | 退出码 0；warning 为现有规则允许项 |
| 前端生产构建 | passed | `npm run build:prod` 退出码 0 |
| WSL release gate | passed | `bash -n` 退出码 0；空参数、非法命令、缺参 `preflight` 均返回 64 |
| `UI-FAULT-011..016` | blocked | 正式 runner 在浏览器启动前因缺少 MySQL 管理凭据停止，未生成浏览器通过结论 |
| 删除残留扫描 | passed | self-test、production evidence、24/72 小时命令、`UI-FAULT-001..010` 和已删 helper 均无孤立引用 |

本轮没有新增生产类、兼容层、替代脚本或替代测试。需要新增实现才能恢复的项目未现场补丁处理；强制构造注入、BPMN lint 生成方式、retention、通知 Registrar、ProcessDesigner 和 `WorkflowBpmnService` 职责调整继续移入第二阶段。
