<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="https://approvaplat.example/flowable/compatibility-integration-test">
  <process id="flowableCompatibilityIntegration"
           name="Flowable 兼容契约集成测试"
           isExecutable="true">
    <startEvent id="start" name="发起申请"
                flowable:formKey="key_compatibility_start"
                flowable:initiator="initiator"/>
    <sequenceFlow id="toReview" sourceRef="start" targetRef="review"/>
    <userTask id="review" name="兼容审批"
              flowable:assignee="${assigneeId}"
              flowable:owner="${ownerId}">
      <extensionElements>
        <flowable:taskListener event="create"
                               delegateExpression="${userTaskListener}"/>
        <flowable:taskListener event="assignment"
                               delegateExpression="${userTaskListener}"/>
        <flowable:taskListener event="complete"
                               delegateExpression="${userTaskListener}"/>
      </extensionElements>
    </userTask>
    <sequenceFlow id="toEnd" sourceRef="review" targetRef="end"/>
    <endEvent id="end" name="结束"/>
  </process>
</definitions>
