<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.flowable.mapper.WfConnectorInvocationMapper">
    <resultMap id="ClaimResult" type="com.ruoyi.flowable.domain.vo.WorkflowConnectorInvocationClaim">
        <constructor>
            <arg column="invocation_id" javaType="java.lang.Long"/>
            <arg column="status" javaType="java.lang.String"/>
            <arg column="claim_token" javaType="java.lang.String"/>
            <arg column="attempt_count" javaType="java.lang.Integer"/>
            <arg column="result_code" javaType="java.lang.Integer"/>
        </constructor>
    </resultMap>

    <insert id="insertIfAbsent">
        insert ignore into wf_connector_invocation
            (deployment_id, process_instance_id, execution_id, element_id, connector_type,
             target_key, target_revision, idempotency_key, operation, target_summary, status,
             attempt_count, create_time, update_time)
        values
            (#{deploymentId}, #{processInstanceId}, #{executionId}, #{elementId}, #{connectorType},
             #{targetKey}, #{targetRevision}, #{idempotencyKey}, #{operation}, #{targetSummary}, 'PENDING',
             0, current_timestamp(3), current_timestamp(3))
    </insert>

    <update id="claim">
        update wf_connector_invocation
        set status = 'RUNNING', attempt_count = attempt_count + 1,
            claim_token = #{claimToken},
            lease_expires_at = timestampadd(second, #{leaseSeconds}, current_timestamp(3)),
            update_time = current_timestamp(3)
        where idempotency_key = #{idempotencyKey}
          and status != 'SUCCESS'
          and (lease_expires_at is null or lease_expires_at &lt;= current_timestamp(3))
    </update>

    <select id="selectClaim" resultMap="ClaimResult">
        select invocation_id, status, claim_token, attempt_count, result_code
        from wf_connector_invocation
        where idempotency_key = #{idempotencyKey}
    </select>

    <update id="completeSuccess">
        update wf_connector_invocation
        set status = 'SUCCESS', duration_ms = #{durationMs}, result_code = #{resultCode},
            result_summary = #{resultSummary}, error_code = null,
            claim_token = null, lease_expires_at = null, update_time = current_timestamp(3)
        where invocation_id = #{invocationId} and claim_token = #{claimToken} and status = 'RUNNING'
    </update>

    <update id="completeFailure">
        update wf_connector_invocation
        set status = 'FAILED', duration_ms = #{durationMs}, result_code = #{resultCode},
            result_summary = #{resultSummary}, error_code = #{errorCode},
            claim_token = null, lease_expires_at = null, update_time = current_timestamp(3)
        where invocation_id = #{invocationId} and claim_token = #{claimToken} and status = 'RUNNING'
    </update>
</mapper>
